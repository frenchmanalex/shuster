const linkPrefix = "/shuster";

const navLink = {
    homepage: linkPrefix + "/",
    productsBikes: linkPrefix + "/products/bikes",
    productsCloths: linkPrefix + "/products/cloths",
    productsShoes: linkPrefix + "/products/shoes",
    contacts: linkPrefix + "/contacts",
    error404: linkPrefix + "/404",
};
export default navLink;
